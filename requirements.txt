# 데이터 파이프라인 (scripts/) 실행 의존성

# OpenAI API
openai==1.12.0

# Supabase (PostgreSQL + pgvector)
supabase==2.3.0

# HTTP & 크롤링
requests==2.31.0
beautifulsoup4==4.12.3
newspaper3k==0.2.8
lxml_html_clean  # newspaper3k 의존 (lxml.html.clean 분리 대응)

# 이미지 처리
Pillow==10.2.0

# 벡터 계산
numpy==1.26.0
scikit-learn==1.3.2

# 환경 변수
python-dotenv==1.0.0
